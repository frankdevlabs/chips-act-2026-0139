# Advisory-body digests

One digest per advisory-body opinion on file 2026/0139 (COD). None adopted yet.

| Body | Status | Source |
|---|---|---|
| **EESC** | Mandatory consultee (cited in the preamble); no opinion page exists yet (checked 10 June 2026) | — |
| **CoR** | Mandatory consultee (cited in the preamble); nothing scheduled yet | — |

When an opinion is adopted: register it (`register-document` skill), commit the file under
[`sources/advisory/`](../../sources/advisory/), and add a digest page here
(`<body>-<reference>.md`), linked from `STATUS.md`'s advisory table. Watch-item: the sibling CADA
file already has an EESC opinion page — when the EESC opens one for Chips Act 2.0, add it to the
[watchlist](../../tracker.yaml) (noted under source T1-08).
